[build]
# Netlify production build: Vite output is published from dist.
# Supabase frontend variables are supplied by the Netlify production environment.
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
