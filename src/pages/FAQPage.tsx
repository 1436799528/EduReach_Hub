import { useState } from 'react'; import { faqs } from '../data/edulebMock'; import { Shell } from '../components/EdulebShared'; export default function FAQPage(){const [open,setOpen]=useState(0);return <Shell title="FAQ"><section className="faq_area section-padding"><div className="container"><div className="faq-list">{faqs.map((f,i)=><div className={`faq-item ${open===i?'open':''}`} key={f.question}><button type="button" onClick={()=>setOpen(open===i?-1:i)}><span>{f.question}</span><i className="fa fa-plus"/></button>{open===i&&<div className="faq-answer"><p>{f.answer}</p></div>}</div>)}</div></div></section></Shell>}