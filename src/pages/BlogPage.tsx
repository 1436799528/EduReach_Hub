import { blogPosts, getBlogPost } from '../data/edulebMock'; import { BlogCard } from '../components/EdulebCards'; import { Shell } from '../components/EdulebShared'; export default function BlogPage(){return <Shell title="Blog"><section className="blog_area section-padding"><div className="container"><div className="row">{blogPosts.map(p=><BlogCard post={p} key={p.id}/>)}</div></div></section></Shell>} export function BlogDetailsPage(){const id=new URLSearchParams(location.search).get('post')||blogPosts[0].id;const p=getBlogPost(id)||blogPosts[0];return <Shell title="Blog Details"><section className="blog_details section-padding"><div className="container"><div className="col-lg-8"><img src={p.image} className="img-fluid" alt=""/><h2>{p.title}</h2><p>{p.excerpt}</p><p>Editable demo article body for the production preview.</p><p><strong>Author:</strong> {p.author}</p></div></div></section></Shell>}