# Eduleb pages

Each file in this folder maps to one visible Eduleb page. The data is intentionally sourced from `src/data/edulebMock.ts` so the demo can be edited now and connected to real data later.
