import { instructors, getInstructor } from '../data/edulebMock'; import { InstructorCard } from '../components/EdulebCards'; import { Shell } from '../components/EdulebShared';
export default function InstructorPage(){return <Shell title="Our instructor"><section className="team_area section-padding"><div className="container"><div className="row">{instructors.map(i=><InstructorCard instructor={i} key={i.id}/>)}</div></div></section></Shell>}
export function InstructorDetailsPage(){const id=new URLSearchParams(location.search).get('instructor')||instructors[0].id;const i=getInstructor(id)||instructors[0];return <Shell title="Instructor Details"><section className="team_details_area section-padding"><div className="container"><div className="row align-items-center"><div className="col-lg-4 text-center"><div className="demo-avatar demo-avatar-large">{i.initials}</div></div><div className="col-lg-8"><div className="section-title"><h2>{i.name}</h2><p>{i.role}</p></div><p>{i.bio}</p><div className="detail-grid"><div><strong>Courses</strong><span>{i.courses}</span></div><div><strong>Students</strong><span>{i.students}</span></div></div></div></div></div></section></Shell>}
