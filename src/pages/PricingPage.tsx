import { pricingPlans } from '../data/edulebMock'; import { Shell } from '../components/EdulebShared'; export default function PricingPage(){return <Shell title="Pricing Plan"><section className="pricing_area section-padding"><div className="container"><div className="row">{pricingPlans.map(p=><div className="col-lg-4" key={p.name}><div className="pricing-table"><h3>{p.name}</h3><div className="price">{p.price}</div><span>{p.period}</span><p>{p.description}</p><ul>{p.features.map(f=><li key={f}><i className="fa fa-check"/> {f}</li>)}</ul><a href="/contact.html" className="btn_one">Choose Plan</a></div></div>)}</div></div></section></Shell>}