import { useState } from 'react'; import { faqs } from '../src/data/edulebMock'; import { Shell } from '../src/components/EdulebShared'; export default function FAQPage(){const [open,setOpen]=useState(0);return <Shell title="Faq"><section className="faq_area section-padding"><div className="container"><div className="row justify-content-center"><div className="col-lg-7 col-sm-12 col-xs-12"><div className="accordion" id="accordionExample">{faqs.map((faq,index)=><div className="accordion-item" key={faq.question}><h2 className="accordion-header" id={`heading${index}`}><button className={`accordion-button ${open===index?'':'collapsed'}`} type="button" onClick={()=>setOpen(open===index?-1:index)} aria-expanded={open===index}>{faq.question}</button></h2><div className={`accordion-collapse collapse ${open===index?'show':''}`}><div className="accordion-body">{faq.answer}</div></div></div>)}</div></div><div className="col-lg-5 col-sm-12 col-xs-12"><div className="faq_img"><img src="https://themewagon.github.io/eduleb/assets/img/faq.jpg" alt="faq image"/></div></div></div></div></section></Shell>}
